$(document).ready(function(){
  //var socket = io.connect();
  //var rooms = ["room1","room2","room3","room4"];

  /*var from = $.cookie('user');//从 cookie 中读取用户名，存于变量 from
	//进入一号房间
 $('#room1').click(function (e) {
    $.post('room0',{user:from,room:'room1'},function(data, status){
      if(status == 'success')
      {
        if(data.errorCode == 0)
        {
          window.location.href = '/room1';
        }
      }
    });
  });*/

});